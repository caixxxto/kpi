name: kpi
description: "kpi"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  dio: ^5.9.2
  auto_route: ^11.1.0
  equatable: ^2.0.8
  dartz: ^0.10.1
  get_it: ^9.2.1
  intl: ^0.20.2
  flutter_gen_runner: ^5.13.0+1
  provider: ^6.1.5+1
  reorderables: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.13.1
  json_serializable: ^6.13.1

flutter:

  uses-material-design: true

